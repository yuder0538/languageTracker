# Mockup 決策

## Metadata

- 功能：搜尋與篩選
- 畫面：單字庫列表（`/vocabulary`）與追劇紀錄列表（`/media-logs`）的搜尋／篩選工具列
- 決策負責人：Niko
- 狀態：待定

## 變體

| 變體 | 說明 | 優點 | 風險 |
|---|---|---|---|
| A | 搜尋框內嵌在標題列右側（新增按鈕左邊），只做文字搜尋，無篩選下拉 | 改動最小、最快上線；畫面不變擠 | 沒有進階篩選，資料量大時仍要靠搜尋關鍵字慢慢縮 |
| B | 標題列下方新增獨立工具列，左搜尋框＋右邊常駐篩選下拉（單字庫：詞性／來源劇集；追劇紀錄：類型／日期區間） | 功能最完整，篩選一次到位 | 實作量較大（含後端新增篩選查詢參數、前端多組下拉狀態）；畫面多一排，資料少時略顯多餘 |

## 設計系統對照

- 重用的 token／元件：`Input`、`Select`、`Button`（皆為既有元件庫 inventory 項目），色彩／間距／圓角皆取自 `design-system.md` S3 token，無新增樣式。
- 新做並登記回 inventory 的元件：無。

## 選定的變體

- 變體：A — 標題列內嵌搜尋框，只做文字搜尋，無篩選下拉
- 為何選這個：改動最小、最快上線；進階篩選（詞性、來源劇集、類型、日期區間）留給後續任務卡，需要時再加。
- 實作前要求的修改：無。

## 人工核准

- 核准者：Niko
- 日期：2026-07-26
- 備註：於 AskUserQuestion 以文字預覽比較兩變體後選定 A。
